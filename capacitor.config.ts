import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.equationpyramid.app',
  appName: 'Equation Pyramid',
  webDir: 'dist'
};

export default config;
